namespace Sample.FinancialMarket.Api.Resources
{
    /// <summary>
    /// This is a marker class. ASP.NET Core Localization uses its type and namespace
    /// to find the corresponding .resx resource files (e.g., SharedResources.en-US.resx).
    /// The class itself is empty and has no logic.
    /// </summary>
    public class SharedResources
    {
    }
}